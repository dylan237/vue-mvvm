class Dylan {
  constructor(options) {
    this.$el = options.el;
    this.$data = options.data;
    this.$options = options;

    if (this.$el) {
      // 1. 實現一個觀察者 observe

      // 2. 實現一個指令解析器 compiler
    }
  }
  
}